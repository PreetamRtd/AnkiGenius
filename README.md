# AnkiGenius

A simple web app that generates Anki flashcards using Google Generative AI. Enter a prompt to create basic or cloze cards and optionally add them to an Anki deck.

Usage:
- Open the app in a browser (serve the `public/` folder).
- Enter a prompt and click "Generate Flashcards".

